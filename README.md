# xKeen Configurator for Home Assistant (HACS) 🛰

Интеграция для управления вашим [XKeen Configurator](https://github.com/mikheys/xkeen-configurator) прямо из Home Assistant.

## ✨ Возможности
*   Добавление доменов в списки маршрутизации через сервисы HA.
*   Автоматическая синхронизация с вашим роутером Keenetic.

## 🚀 Установка

### Через HACS (Рекомендуется)
1. Откройте **HACS** в вашем Home Assistant.
2. Нажмите на три точки в верхнем правом углу и выберите **Custom repositories** (Пользовательские репозитории).
3. Вставьте ссылку: `https://github.com/mikheys/ha-xkeen-configurator`
4. В поле "Category" выберите **Integration**.
5. Нажмите **Add** и затем установите появившуюся интеграцию **xKeen**.

### Ручная установка
Скопируйте папку `custom_components/xkeen` в папку `/config/custom_components/` вашего Home Assistant.

## ⚙️ Настройка

Добавьте следующий блок в ваш `configuration.yaml`:

```yaml
xkeen:
  url: "http://YOUR_MINIPC_IP:3000"
  token: "your-secret-token"
```

## 🛠 Сервисы

Интеграция предоставляет сервис `xkeen.add_domain`:
*   `domain`: (обязательно) Домен для добавления.
*   `rule_index`: (опционально) Индекс правила в JSON (по умолчанию 2).
